import { UserController } from './controllers/UserCtrollers';
const appElement : HTMLElement = document.querySelector('#app') as HTMLElement;
console. log('Login form');
if (appElement){ let usercontroller: UserController = new UserController(appElement);
}